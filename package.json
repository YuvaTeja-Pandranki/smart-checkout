{
  "name": "smart-checkout",
  "version": "1.0.0",
  "description": "Serverless checkout service — Smart Order Fulfillment",
  "main": "dist/handlers/checkout.js",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "jest --coverage",
    "test:watch": "jest --watch",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.137",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.12.7",
    "esbuild": "^0.28.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.583.0",
    "@aws-sdk/lib-dynamodb": "^3.583.0"
  }
}
